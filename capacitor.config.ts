import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.codize.odoo_barcode',
  appName: 'odoo-barcode',
  webDir: 'dist/odoo-barcode',
  bundledWebRuntime: false
};

export default config;
